services:
  - type: web
    name: freelance-crm
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: CRM_SECRET_KEY
        generateValue: true
